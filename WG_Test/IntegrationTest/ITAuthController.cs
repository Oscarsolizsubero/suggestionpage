using Microsoft.AspNetCore.Hosting;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using System.Text;
using WishGrid.Controllers;
using WishGrid.IRepositories;
using WishGrid.RepositoriesEF;
using WishGrid.ViewModels;
using WishGrid.Models;

namespace WishGrid.Tests
{
    [TestClass]
    public class ITAuthController
    {
        //[TestCategory("AuthController")]
        //[TestMethod]
        //public void TestVerifyEncryption()
        //{
        //    var context = Program.BuildWebHost(new string[] { });
        //    context.Run();
        //    using (context)
        //    {
        //        var x= context.Services;
        //        //AuthController controller = new AuthController(new REFUser(new DataContext()), null);
        //        //VMUserAuth user = new VMUserAuth();
        //        //var actionResult = controller.Login(user);

        //    }

        //}
    }
}
